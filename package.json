{
  "name": "homelib-packages",
  "private": true,
  "license": "MIT",
  "author": "vilicvane",
  "type": "module",
  "scripts": {
    "3": "pnpm install && pnpm dedupe && pnpm install",
    "build": "rimraf --glob packages/*/{.bld,bld} && tsc --build",
    "lint": "eslint --no-error-on-unmatched-pattern . && run-in-every eslint-project --parallel --echo -- eslint --no-error-on-unmatched-pattern .",
    "lint-prettier": "prettier --check .",
    "test": "pnpm lint-prettier && pnpm build && pnpm lint",
    "core": "pnpm --filter @homelib/core",
    "dashboard": "pnpm --filter @homelib/dashboard",
    "http": "pnpm --filter @homelib/http",
    "universal": "pnpm --filter @homelib/universal",
    "xiaomi": "pnpm --filter @homelib/xiaomi",
    "homelib": "pnpm --filter homelib"
  },
  "devDependencies": {
    "@mufan/eslint-plugin": "^0.2.10",
    "eslint": "^8.52.0",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5",
    "run-in-every": "^0.2.0",
    "typescript": "^5.2.2"
  }
}
