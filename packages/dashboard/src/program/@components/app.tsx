import {Router} from 'boring-router';
import type {ReactElement} from 'react';
import React from 'react';

export type AppProps = {};

export const App = ({}: AppProps): ReactElement => {
  return <>test</>;
};
